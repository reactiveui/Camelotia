<UserControl
    x:Class="Camelotia.Presentation.Uwp.Views.AuthView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Camelotia.Presentation.Uwp.Views"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <Pivot>
        <PivotItem>
            <PivotItem.Header>
                <TextBlock Text="Direct Auth" Visibility="{x:Bind ViewModel.SupportsDirectAuth, Mode=OneWay}" />
            </PivotItem.Header>
            <views:DirectAuthView ViewModel="{x:Bind ViewModel.DirectAuth, Mode=OneWay}" />
        </PivotItem>
        <PivotItem>
            <PivotItem.Header>
                <TextBlock Text="OAuth" Visibility="{x:Bind ViewModel.SupportsOAuth, Mode=OneWay}" />
            </PivotItem.Header>
        </PivotItem>
    </Pivot>
</UserControl>

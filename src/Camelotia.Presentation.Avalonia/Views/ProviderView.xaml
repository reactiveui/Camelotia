<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Camelotia.Presentation.Avalonia.Views"
             xmlns:interfaces="clr-namespace:Camelotia.Presentation.Interfaces;assembly=Camelotia.Presentation"
             xmlns:designTime="clr-namespace:Camelotia.Presentation.DesignTime;assembly=Camelotia.Presentation"
             x:Class="Camelotia.Presentation.Avalonia.Views.ProviderView">
    <Design.DataContext>
        <designTime:DesignTimeProviderViewModel />
    </Design.DataContext>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
              <Button Grid.Column="0"
                      Command="{Binding Back}"
                      ToolTip.Tip="Back"
                      Classes="Primary">
                <DrawingPresenter Height="16">
                  <DrawingPresenter.Drawing>
                    <GeometryDrawing Brush="{DynamicResource ThemeForegroundBrush}" Geometry="M 34,57L 42,57L 42,32.25L 52,42.25L 52,31.75L 38,17.75L 24,31.75L 24,42.25L 34,32.25L 34,57 Z "   />
                  </DrawingPresenter.Drawing>
                </DrawingPresenter>
              </Button>
                <TextBlock Grid.Column="1"
                         IsVisible="{Binding !ShowBreadCrumbs}"
                         Text="{Binding CurrentPath}"
                         VerticalAlignment="Center"
                         FontSize="14"
                         Margin="15 0" />
                <ListBox Grid.Column="1"
                         IsVisible="{Binding ShowBreadCrumbs}"
                         Items="{Binding BreadCrumbs}"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden"
                         ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                         Margin="5">
                  <ListBox.Styles>
                    <Style Selector="ListBox > Border">
                      <Setter Property="CornerRadius" Value="5"/>
                      <Setter Property="Background" Value="{DynamicResource ThemeCardBrush}"/>
                    </Style>
                    <Style Selector="Button">
                      <Setter Property="Margin" Value="0"/>
                      <Setter Property="Padding" Value="5,0,0,0"/>
                      <Setter Property="VerticalContentAlignment" Value="Center"/>
                      <Setter Property="Background" Value="Transparent"/>
                    </Style>
                    <Style Selector="Menu">
                      <Setter Property="Padding" Value="0"/>                      
                      <Setter Property="Background" Value="Transparent"/>                      
                      <Setter Property="Foreground" Value="{DynamicResource ThemeForegroundBrush}"/>
                    </Style>
                    <Style Selector="MenuItem">
                      <Setter Property="Padding" Value="0"/>
                      <Setter Property="Margin" Value="2,0"/>                      
                    </Style>
                    <Style Selector="MenuItem:pointerover /template/ Border#root">
                      <Setter Property="Background" Value="{DynamicResource HighlightBrush}" />
                    </Style>
                    <Style Selector="MenuItem /template/ Border#root">
                      <Setter Property="CornerRadius" Value="5" />
                    </Style>
                    <Style Selector="ListBoxItem">
                      <Setter Property="Padding" Value="0"/>
                      <Setter Property="Margin" Value="0"/>
                      <Setter Property="BorderThickness" Value="0"/>
                    </Style>                
                    <Style Selector="TextBlock">                      
                      <Setter Property="VerticalAlignment" Value="Center"/>                      
                    </Style>
                  </ListBox.Styles>
                  <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                      <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                  </ListBox.ItemsPanel>
                  <ListBox.ItemTemplate>
                    <DataTemplate>
                      <Button Command="{Binding Provider.SetPath}" CommandParameter="{Binding FullPath}">
                        <StackPanel Orientation="Horizontal">
                          <TextBlock Text="{Binding Name}"/>
                          <Menu>
                            <MenuItem Items="{Binding Children}">
                              <MenuItem.Header>
                                <DrawingPresenter Height="16" Width="8">
                                  <DrawingPresenter.Drawing>
                                    <GeometryDrawing Brush="{DynamicResource ThemeForegroundBrush}" Geometry="F1M257.569366455078,-475.600006103516C264.498016357422,-474.88818359375,270.815612792969,-471.318115234375,275,-465.75L395.75,-315.75C403.332916259766,-306.524963378906,403.332916259766,-293.225036621094,395.75,-284L270.75,-134C265.663421630859,-127.863845825195 257.955871582031,-124.520812988281 250,-125 244.15869140625,-124.988586425781 238.497802734375,-127.022972106934 234,-130.75C228.887329101563,-134.988708496094,225.671310424805,-141.087524414063,225.061584472656,-147.700714111328C224.451843261719,-154.313903808594,226.49853515625,-160.897933959961,230.75,-166L342.75,-300 234.75,-434.25C230.555740356445,-439.414855957031,228.593276977539,-446.038543701172,229.297103881836,-452.654571533203C230.000946044922,-459.270599365234,233.313003540039,-465.333190917969,238.5,-469.5C243.72917175293,-474.100952148438,250.640686035156,-476.311859130859,257.569366455078,-475.600006103516z"   />
                                  </DrawingPresenter.Drawing>
                                </DrawingPresenter>
                              </MenuItem.Header>
                              <MenuItem.Styles>
                                <Style Selector="MenuItem">
                                  <Setter Property="Header" Value="{Binding Name}"/>
                                  <Setter Property="Command" Value="{Binding Provider.SetPath}"/>
                                  <Setter Property="CommandParameter" Value="{Binding FullPath}"/>
                                </Style>
                              </MenuItem.Styles>
                            </MenuItem>
                          </Menu>
                        </StackPanel>
                      </Button>
                    </DataTemplate>
                  </ListBox.ItemTemplate>                
                </ListBox>              
                <Button Grid.Column="2"
                        Command="{Binding Open}"
                        HorizontalAlignment="Stretch"
                        ToolTip.Tip="Open"
                        Classes="Primary">
                  <DrawingPresenter Height="16">
                    <DrawingPresenter.Drawing>
                      <GeometryDrawing Brush="{DynamicResource ThemeForegroundBrush}" Geometry="M 19.0002,34L 19.0002,42L 43.7502,42L 33.7502,52L 44.2502,52L 58.2502,38L 44.2502,24L 33.7502,24L 43.7502,34L 19.0002,34 Z "/>
                    </DrawingPresenter.Drawing>
                  </DrawingPresenter>
                </Button>
            </Grid>
            <Border Grid.Row="1" 
                    BorderThickness="0 2 0 2"
                    BorderBrush="{DynamicResource ThemeBorderLowBrush}">
                <Grid>
                    <ListBox Padding="0"
                             Background="{DynamicResource ThemeCardBrush}"
                             BorderThickness="0"
                             SelectionMode="Toggle"
                             Items="{Binding Files}"
                             IsVisible="{Binding IsReady}"
                             ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                             SelectedItem="{Binding SelectedFile, Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="interfaces:IFileViewModel">
                                <views:FileView DataContext="{Binding}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Grid IsVisible="{Binding HasErrorMessage}" 
                          Background="{DynamicResource ThemeCardBrush}">
                        <StackPanel VerticalAlignment="Center">
                            <Ellipse Width="10" Height="10"
                                     HorizontalAlignment="Center"
                                     Fill="#ff0000"
                                     Margin="10" />
                            <TextBlock HorizontalAlignment="Center"
                                       Text="Can't perform the operation, an error has occured."/>
                        </StackPanel>
                    </Grid>
                    <Grid IsVisible="{Binding IsCurrentPathEmpty}" Background="{DynamicResource ThemeCardBrush}">
                        <StackPanel VerticalAlignment="Center">
                            <Ellipse Width="10" Height="10"
                                     HorizontalAlignment="Center"
                                     Fill="#ff0000"
                                     Margin="10" />
                            <TextBlock HorizontalAlignment="Center"
                                       Text="Whoops, no files here!" />
                        </StackPanel>
                    </Grid>
                    <Grid IsVisible="{Binding IsLoading}" 
                          Background="{DynamicResource ThemeCardBrush}">
                        <StackPanel VerticalAlignment="Center">
                            <ProgressBar HorizontalAlignment="Stretch"
                                         IsIndeterminate="{Binding IsLoading}" />
                            <TextBlock HorizontalAlignment="Center"
                                       Text="Please, wait..." />
                        </StackPanel>
                    </Grid>
                    <Grid IsVisible="{Binding Folder.IsVisible}" 
                          Background="{DynamicResource ThemeCardBrush}">
                        <views:CreateFolderView DataContext="{Binding Folder}" />
                    </Grid>
                    <Grid IsVisible="{Binding Rename.IsVisible}" 
                          Background="{DynamicResource ThemeCardBrush}">
                        <views:RenameFileView DataContext="{Binding Rename}" />
                    </Grid>
                </Grid>
            </Border>
            <Grid Grid.Row="2" Margin="5 5 5 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="Delete"
                        Classes="Primary"
                        HorizontalAlignment="Stretch"
                        Command="{Binding DeleteSelectedFile}" />
                <Button Grid.Column="1"
                        Content="Rename"
                        Classes="Primary"
                        HorizontalAlignment="Stretch"
                        Command="{Binding Rename.Open}"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" 
                            Margin="5" VerticalAlignment="Center">
                    <TextBlock Text="{Binding SelectedFile.Name}"
                               Opacity="0.5" />
                </StackPanel>
                <Button Grid.Column="3"
                        Content="Upload"
                        Classes="Primary"
                        HorizontalAlignment="Stretch"
                        Command="{Binding UploadToCurrentPath}" />
                <Button Grid.Column="4"
                        Content="Download"
                        Classes="Primary"
                        HorizontalAlignment="Stretch"
                        Command="{Binding DownloadSelectedFile}" />
            </Grid>
            <Grid Grid.Row="3" Margin="5 0 5 5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="Logout"
                        Classes="Primary"
                        HorizontalAlignment="Stretch"
                        Command="{Binding Logout}" />
                <Button Grid.Column="1"
                        Content="Unselect"
                        Classes="Primary"
                        HorizontalAlignment="Stretch"
                        Command="{Binding UnselectFile}" />
                <StackPanel Grid.Column="2" Orientation="Horizontal" 
                            Margin="5" VerticalAlignment="Center">
                    <TextBlock Text="Refreshing in: " />
                    <TextBlock Text="{Binding RefreshingIn}" />
                </StackPanel>
                <Button Grid.Column="3" 
                        Content="Create" 
                        Classes="Primary"
                        HorizontalAlignment="Stretch" 
                        Command="{Binding Folder.Open}"/>
                <Button Grid.Column="4"
                        Content="Refresh"
                        HorizontalAlignment="Stretch"
                        Command="{Binding Refresh}" />
            </Grid>
        </Grid>
        <Grid IsVisible="{Binding Auth.IsAnonymous}">
            <views:AuthView DataContext="{Binding Auth}" />
        </Grid>
    </Grid>
</UserControl>
